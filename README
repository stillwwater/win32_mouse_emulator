Win32 Mouse Emulator
====================

Record mouse input to a file, and play it back in Windows.


Usage
-----

Start a recording:

```shell
mouse_emu record output_path [input_lag (ms)]
```

Play a recording:

```shell
mouse_emu play input_path [input_lag (ms)] [speed]
```
